name: Test Trigger

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test workflow
        run: echo "Workflow triggered successfully"